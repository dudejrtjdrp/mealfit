import { Text, View } from 'react-native';

/** A2 로그인 — 시안 docs/design/A2-login.png */
export default function LoginScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>A2 로그인 (구현 예정)</Text>
    </View>
  );
}
